import createTheme from "@mui/material/styles/createTheme";

const theme = createTheme({
    colorSchemes: {
        dark: true,
    },
    typography: {
        overline: {
            fontSize: "1rem",
        },
    },
});

export default theme;
