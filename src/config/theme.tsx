import createTheme from "@mui/material/styles/createTheme";

const theme = createTheme({
    // colorSchemes: {
    //     dark: true,
    // },
    typography: {
        overline: {
            fontSize: "1rem",
        },
        body1: {
            fontSize: "1.2rem",
        },
    },
});

export default theme;
